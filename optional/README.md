# Optional Layers

This baseline keeps core settings minimal. Add these only if useful:

- Full personal `.obsidian` preferences (themes, hotkeys, plugin configs)
- `.claude/` helper files or skills for Claude-specific workflows
- Extra agent-specific config files for other tools

Keep these optional so the template stays portable across machines and teams.
